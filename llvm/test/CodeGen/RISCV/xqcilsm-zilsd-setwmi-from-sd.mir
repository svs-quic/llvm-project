# NOTE: Assertions have been autogenerated by utils/update_mir_test_checks.py UTC_ARGS: --version 6
# RUN: llc -mtriple=riscv32 -mattr=+zilsd,+xqcilsm -run-pass=riscv-load-store-opt %s -o - | FileCheck %s

--- |
  define void @sd_zero_neg_offsets() { ret void }
...
---
name:            sd_zero_neg_offsets
tracksRegLiveness: true
body:             |
  bb.0:
    liveins: $x8
    ; CHECK-LABEL: name: sd_zero_neg_offsets
    ; CHECK: liveins: $x8
    ; CHECK-NEXT: {{  $}}
    ; CHECK-NEXT: $x8 = ADDI killed $x8, -64
    ; CHECK-NEXT: QC_SETWMI $x0, killed $x8, 8, 0
    ; CHECK-NEXT: PseudoRET
    SD_RV32 $x0_pair, $x8, -40 :: (store (s64), align 8)
    SD_RV32 $x0_pair, $x8, -48 :: (store (s64), align 8)
    SD_RV32 $x0_pair, $x8, -56 :: (store (s64), align 8)
    SD_RV32 $x0_pair, killed $x8, -64 :: (store (s64), align 8)
    PseudoRET
...

